# Feature 26: Session Settings Window

Add a gear icon after the session name in the game page to open a session
settings window. This icon and window are only visible to the GM.

In the window the session can be renamed. Two color schemes can be defined (one
light color scheme and one dark color scheme). The dark scheme defaults to the
blue one currently in use. The light color scheme is a sandy brown color.

## Status: Not Started
