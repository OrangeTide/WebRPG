# Feature 11: Dockerfile

Create a Dockerfile for running the server (Alpine Linux based or distroless
container). Publish releases to Docker Hub.

## Status: In Progress

A Dockerfile exists in `WIP/Dockerfile` (multi-stage build) but is not
production-ready. No docker-compose.yml. Not yet publishing to Docker Hub.

## Plan

TBD

## Findings

- Multi-stage Dockerfile exists at `WIP/Dockerfile`
- Needs review and promotion from WIP to project root
- Docker Hub publishing workflow not yet created
